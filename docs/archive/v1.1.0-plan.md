# v1.1.0 — AI Agent Integration (MCP Server)

> **Status:** ✅ Shipped — v1.1.0 is the current stable release.
> **Base:** `main` at `215c480`.
> **Tag:** `v1.1.0`

---

## What Shipped

### MCP Server for AI Agent PDF Integration

- **`pdfreader_lib/mcp_server.py`** — Model Context Protocol server exposing 14 PDF operations as tools for AI agents:
  - **Reading:** `extract_text`, `get_page_text`, `get_metadata`, `get_page_count`
  - **Search:** `search_pdf` (single doc), `search_library` (FTS5 BM25), `search_semantic` (TF-IDF)
  - **Compare:** `compare_pdfs` (page-by-page structured diff with summary)
  - **Transform:** `merge_pdfs`, `split_pdf`, `extract_pages`, `compress_pdf`
  - **Index:** `index_folder`, `list_indexed_docs`
- **Transports:** stdio (default, for agent CLIs) and SSE (optional, for HTTP gateways)
- **`requirements-mcp.txt`** — optional dependency pin (`mcp>=1.0.0`)
- The MCP server uses existing `pdfreader_lib/` modules (no code changes to the GUI).
- All operations are local. No cloud dependencies.

### README Refresh

- New "AI Agent Integration" section with setup and agent configuration guide.
- Features table expanded with missing capabilities (annotation management, Save PDF, keyboard shortcuts, diff summary).
- Tech stack expanded from 3 to 10 rows with actual versions.
- AGENTS.md updated with MCP server maintenance rules.

---

## Deferred to v1.2.0

The following items from the original v1.1.0 plan were deferred:

- Better tab styling and visual polish
- Recent files / start screen
- Thumbnail / sidebar exploration
- Updated UI screenshots
- Rendering quality refinements
- Signed Windows binaries research
- macOS DMG packaging follow-up

See [v1.2.0-plan.md](./v1.2.0-plan.md) for the next milestone.
